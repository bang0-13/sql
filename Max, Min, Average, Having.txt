CREATE TABLE MEALS ( NAME VARCHAR(255), PRICE DOUBLE,  CALORIES INT);
INSERT INTO MEALS (NAME, PRICE, CALORIES) VALUES
("Spaghetti pummarola", 5.50, 600), ("Cacio e pepe", 15.00, 700), ("Carbonara", 18.00, 800), ("Amatriciana", 8.00, 700);



-- L'esercizio inizia qui
-- 1)
SELECT NAME, MAX(PRICE), CALORIES FROM MEALS GROUP BY CALORIES, NAME HAVING CALORIES > 500;
-- 2)
SELECT NAME, PRICE, AVG(CALORIES) FROM MEALS GROUP BY NAME, PRICE HAVING PRICE > 15.00;
-- 3) è l'esercizio che abbiamo fatto in live, che non ha una consegna corretta

-- 4)
SELECT NAME, PRICE, AVG(PRICE) FROM MEALS GROUP BY NAME, PRICE HAVING PRICE < AVG(PRICE);
-- 5)
SELECT NAME, PRICE, AVG(PRICE), CALORIES FROM MEALS GROUP BY NAME, PRICE, CALORIES
HAVING (PRICE < AVG(PRICE) AND CALORIES > 600);